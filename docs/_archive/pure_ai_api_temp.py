"""Legacy shim for standalone pure AI API.

Implementation lives in `apis.pure_ai_api`.
This module remains only to satisfy legacy imports (`pure_ai_api:app`).
Safe to remove after external references are updated.
"""
