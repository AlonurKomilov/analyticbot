{
  "name": "Analytic Bot Codespace",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",

  // Docker-in-Docker: runs dockerd inside the container
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {
      "moby": true
    }
  },

  // Post-create: install Poetry + add user to docker group
  "postCreateCommand": "pipx install poetry && poetry config virtualenvs.in-project true && poetry install --no-root && sudo usermod -aG docker $USER || true",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-azuretools.vscode-docker"
      ],
      "settings": {
        "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
        "python.terminal.activateEnvironment": true,
        "python.analysis.autoImportCompletions": true,
        "python.analysis.extraPaths": ["${workspaceFolder}"]
      }
    }
  },

  // Useful ports (optional): API, Postgres, Redis
  "forwardPorts": [10400, 5432, 6379],

  // Named volume to persist Docker data inside container (survives rebuilds)
  "mounts": [
    "source=dind-var-lib-docker,target=/var/lib/docker,type=volume"
  ]
}
