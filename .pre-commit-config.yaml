repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      # Temporarily disabled for git sync - will re-enable after
      #- id: check-yaml
      - id: check-added-large-files
      - id: check-merge-conflict
      - id: debug-statements

  # Temporarily disabled for git sync - will re-enable after
  #- repo: https://github.com/astral-sh/ruff-pre-commit
  #  rev: v0.6.9
  #  hooks:
  #    - id: ruff
  #      args: [--fix]
  #    - id: ruff-format

  # Temporarily disabled for git sync - will re-enable after
  #- repo: https://github.com/pre-commit/mirrors-mypy
  #  rev: v1.10.0
  #  hooks:
  #    - id: mypy
  #      exclude: ^(tests/|bot/database/migrations/|docs/|scripts/|apps/mtproto/|apps/bot/services/|apps/bot/middlewares/|apps/api/|core/services/payment/|infra/)
  #      args: [--ignore-missing-imports, --no-strict-optional, --allow-untyped-calls, --allow-untyped-defs]

  - repo: local
    hooks:
      - id: forbid-cross-imports
        name: 🛡️  Clean Architecture Import Guard
        entry: python3 scripts/guard_imports.py
        language: system
        types: [python]
        description: "Prevent core/ from importing apps/ or infra/ modules"
        exclude: ^(core/services/payment/|tests/|bot/database/migrations/|docs/|scripts/)
        pass_filenames: false
        always_run: true
