# MTProto Integration Configuration Example
# Copy this to your .env file and configure as needed

# ============= MTProto Feature Flag =============
# Enable/disable MTProto functionality (disabled by default for safety)
MTPROTO_ENABLED=false

# ============= Telegram API Credentials =============
# Required when MTPROTO_ENABLED=true
# Get these from https://my.telegram.org/apps
TELEGRAM_API_ID=
TELEGRAM_API_HASH=

# ============= Session Configuration =============
# Session file name for Telegram client
TELEGRAM_SESSION_NAME=mtproto_session

# ============= Proxy Configuration (Optional) =============
# Uncomment and configure if you need proxy support
# TELEGRAM_PROXY=socks5://user:pass@host:port

# ============= Logging Configuration =============
# Logging level for MTProto application
LOG_LEVEL=INFO

# ============= Docker Compose Profiles =============
# To start MTProto service with Docker Compose:
# docker-compose --profile mtproto up mtproto
#
# To start all services including MTProto:
# docker-compose --profile mtproto up

# ============= Development Notes =============
# This is Phase 4.1 MTProto Foundation - stub implementation
# - MTProto functionality is feature-flagged (disabled by default)
# - No actual Telegram connections are made in this phase
# - This provides the foundation for future MTProto integration
# - To enable: set MTPROTO_ENABLED=true and provide API credentials
