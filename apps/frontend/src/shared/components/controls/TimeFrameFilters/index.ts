/**
 * TimeFrameFilters - Unified time frame filter controls
 * 
 * @example
 * ```tsx
 * import { TimeFrameFilters } from '@shared/components/controls';
 * <TimeFrameFilters timeFrame="30d" setTimeFrame={handleChange} />
 * ```
 */

export { default } from './TimeFrameFilters';
export { default as TimeFrameFilters } from './TimeFrameFilters';
export * from './types';
