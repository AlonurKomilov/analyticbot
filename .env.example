# --- runtime / bot ---
BOT_TOKEN=replace_me
ADMIN_IDS=123456789
DEFAULT_LOCALE=en
STORAGE_CHANNEL_ID=-1000000000000

# --- postgres ---
# These variables are used to construct the DATABASE_URL if it's not set explicitly.
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=analytic
POSTGRES_PASSWORD=change_me
POSTGRES_DB=analytic_bot

# --- URLs / ports ---
# The application uses the asyncpg driver, while Alembic uses psycopg2.
# The `alembic/env.py` script automatically reads this environment variable.
# Local development on host machine (when running Postgres locally):
DATABASE_URL=postgresql+asyncpg://analytic:change_me@localhost:5432/analytic_bot

# IMPORTANT: When using docker compose containers, override the host to `postgres`.
# You can do this either by editing the line above OR by adding the line below.
# (If both are present the last non-comment wins.)
# DATABASE_URL=postgresql+asyncpg://analytic:change_me@postgres:5432/analytic_bot

REDIS_URL=redis://redis:6379/0
# If you prefer separate host/port instead of full URL, uncomment below.
# REDIS_HOST=redis
# REDIS_PORT=6379

API_HOST=0.0.0.0
API_PORT=8000
API_HOST_URL=http://localhost:8000

# Telegram Web App **must** be a secure (HTTPS) URL.
# Use a real HTTPS URL from a service like ngrok, Codespaces, or Vercel.
WEBAPP_URL=https://your-public-https-host.example/

TWA_HOST_URL=http://localhost:5173

# --- feature flags / misc ---
ENFORCE_PLAN_LIMITS=True
SENTRY_DSN=

# --- security / auth ---
JWT_SECRET_KEY=change_me_min_32_chars
JWT_REFRESH_SECRET_KEY=change_me_refresh_min_32_chars

# --- external services ---
OPENAI_API_KEY=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_USE_TLS=True
SMTP_FROM_EMAIL=no-reply@example.com

